@model ADSBackend.Models.PassRecord

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>PassRecord</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PassId" />
            <div class="form-group">
                <label asp-for="Reason" class="control-label"></label>
                <input asp-for="Reason" class="form-control" />
                <span asp-validation-for="Reason" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IsssuedDate" class="control-label"></label>
                <input asp-for="IsssuedDate" class="form-control" />
                <span asp-validation-for="IsssuedDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="memberId" class="control-label"></label>
                <select asp-for="memberId" class="form-control" asp-items="ViewBag.memberId"></select>
                <span asp-validation-for="memberId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
